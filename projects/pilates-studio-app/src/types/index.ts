/**
 * Tipos globales de StudioFlow.
 * Vacío por ahora (Fase 1A): las entidades de dominio (studios, members,
 * classes, reservations, packs, payments…) se definen en Fase 1 junto con
 * el schema. Ver docs/database.md.
 */
export {};
